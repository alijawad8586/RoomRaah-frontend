# Frontend workspace

The rules for this directory live in **[`AGENTS.md`](./AGENTS.md)** — read it before
touching anything here. It is the same file every other agent tool reads, kept in one place
so the rules cannot drift between them.

The repository-level `CLAUDE.md` at the root describes the **backend**, which is finished
and is not edited from here.
